# Ignore all certificate and key files in this directory
# (Certificates are now accessed via the certs/ symlink)
*.crt
*.key
*.pem
*.p12
*.pfx

# Ignore realm configurations except the default crucible realm
*realm*.json
!crucible-realm.json

# Ignore environment-specific files
*.env
*.local
